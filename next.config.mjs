/** @type {import('next').NextConfig} */
const nextConfig = {
    // Build to static web
    output: 'export',
    // Optional: Change the output directory `out` -> `dist`
    distDir: 'dist',
};

export default nextConfig;
